- The system initially had a default user: ubuntu. 
- From this user session
  The root password was set using the passwd command. 
  In the SSH configuration file /etc/ssh/sshd_config, the parameter PermitRootLogin was set to yes to allow SSH login as root. 
- The system was then rebooted.
